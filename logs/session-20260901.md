# Session Log — 2026-09-01 — Wiki refresh round 2 (Watcher retirement sweep, SOP numbering)

Scope: documentation only — no code, config, or detection changes. Wiki
pushed directly (wikis have no PR mechanism); repo-side changes via the
companion PR that carries this log.

## 1. Wiki refresh (wiki commit `25ab241`)

Implemented the owner-provided refresh set
(`UIW-SOC/wiki update/`, 6 files) against `voltron-1/Suburban_SOC.wiki`:

- **Evidence-Log**, **Sprint-Notes** — point-in-time preservation notes
  added: Watcher mentions in historical rows are accurate to their run,
  retired since by #267; not current architecture.
- **Final-Report** — SOAR section corrected from "Kibana Watcher" to the
  ingest-time HMAC-signed Logstash trigger (#267); Challenges/Future
  Enhancements updated — SSH brute-force trigger done, port-scan
  deliberately dashboard-only (#331).
- **SOP-022-anomaly-validation** — "ensure the Kibana Watcher is
  installed" steps replaced with the ingest-time trigger path (#267);
  Watcher JSON reference repointed to `rules/elastic_watcher/retired/`.
- **SOP-147-evidence-validation-runbook** — Watcher line corrected to the
  HMAC-signed trigger + approval-queue path (#267).
- **SOP-Pipeline-Operations** — script-to-SOP-number quick-reference
  mapping retired (collided with the wiki's compliance SOP-003..013
  pages — #523); stale `sterlinggarnett/cis3353_s26_TL_SG_MF` links
  repointed to `voltron-1/Suburban_SOC`; startup sequence's "install
  Watcher" step replaced.

Fact-check against the repo found one stale claim in the provided files,
corrected before push (same precedent as the 2026-08-29 Home.md refresh):
SOP-147's validator count 22/22 → **24/24**
(`python3 tests/validate_emulation_map.py` → "Summary: 24 scenarios 24
clean 0 warn 0 fail"). All other spot-checked claims verified: `#286`
local.zeek dead-file status matches the file's own header, referenced
scripts (`run_all.sh`, `preflight.sh`, `verify_quarantine.sh`,
`rules/elastic_watcher/retired/soar_quarantine_alert.json`) all exist.

Note: the CLAUDE.md ops note "the wiki cannot be pushed to from a Claude
Code session" describes the cloud-session git proxy; this local WSL
session pushes the wiki fine over HTTPS (as did the 2026-08-29 session,
wiki commits `1d95dfb`/`193166f`).

## 2. Issue mapping (M25 — Repo Audit Remediation)

- **#523** (SOP quick-reference numbering collision) — fully implemented
  by the SOP-Pipeline-Operations rewrite → closed.
- **#514** (repo `docs/SOP-147-evidence-validation-runbook.md:30` missed
  the #267 sweep) — the wiki page mirrors this repo doc, so the same
  one-line correction is applied repo-side in this PR → closed.
- **#522** (6 Watcher-era wiki pages) — 5/6 done this session. The
  provided set included Evidence-Log (not on #522's list) but not
  **Commit-Approach**, which remains stale → #522 stays open, progress
  comment posted.

## 3. planned_execution.md

- Added the M25 row (milestone/30, 16 issues, created 2026-08-31) to the
  NEXT UP milestone table with this session's 2/16 progress; intro line
  updated to M23/M24/M25.

End state: wiki fully de-Watchered except Commit-Approach (#522); open
actionable work is M23's merge queue (PRs #506/#507 + Stage 3) and M25's
remaining 14 issues.
