# Blue team analysis conventions
- Never assert an IOC is malicious without a cited source (VT, MISP, internal case).
- If log evidence is ambiguous or incomplete, say so — never guess intent or attribution.
- Map to ATT&CK technique IDs only when clearly evidenced in the provided data.
- Never invent CVEs, hashes, or timestamps — quote verbatim from the source log/report.

# Session/ops notes (as of 2026-09-01)
- **Milestone tracking lives in `planned_execution.md` and `README.md`** (repo-tracked, updated after every merge) — check those first for current per-issue status before re-deriving it from the GitHub API.
- **The GitHub wiki (`suburban_soc.wiki`) cannot be pushed to from a Claude Code session** — confirmed directly (git proxy denies it: "not in this session's authorized repository set"; `add_repo` also fails, repo not found). To update it: clone read-only (`git clone https://github.com/voltron-1/suburban_soc.wiki`), edit `Home.md` locally, and hand the file to the user (e.g. via a file send) to paste into the wiki UI themselves. The wiki *has* been updated this way before (someone pasted a prior draft) — it's a live, real doc, just not one this session can write to directly.
- **Milestone status as of 2026-09-01**: M17 (Detection Rule Coverage & Correctness) is fully done — 30/34 closed, 4 permanently not-actionable (#283, #333 — externally blocked/speculative; #415, #421 — need a live Docker+Zeek+ES stack this sandbox doesn't have). M18 (ECS Pipeline & Field-Mapping Integrity) has 6 open issues (#326, #396, #403, #405, #389, #390), all blocked/deferred/scoped-out — no actionable work left. M24 (security follow-ups, 5 issues) closed 2026-08-30 via PR #505. M23 (Suricata signature lane, 4 issues) landed via PR #506/#507 (both merged 2026-08-30): #445 closed; #443/#444 are structurally complete but stay open — their own "done when" needs a live capture host + real traffic this sandbox doesn't have, same blocker class as M17's #415/#421; #446 (100-rule starter set, landed disabled) has real remaining scope — per-rule pcap fixtures, placeholder-IOC resolution, threshold tuning, DLP owner sign-off.
- **A PR body with "Closes #X, #Y" (comma-separated, one keyword) does not reliably auto-close both issues** — GitHub sometimes only closes the first. Always verify each referenced issue individually after merging and close stragglers manually (`state: closed, state_reason: completed`). In practice this repo's recent PRs (#505, #506, #507) don't use auto-close keywords at all — every issue on them was closed manually, 15-105s after merge, precisely to avoid relying on the unreliable behavior above. Keep doing that: don't reintroduce `Closes #X`/`Fixes #X` keywords on the assumption they're now safe: the underlying GitHub behavior this note warns about hasn't changed, only this repo's practice of avoiding it.
- **This sandbox has no live Docker daemon, Elasticsearch, or Kibana** — `docker version` succeeds for the client but fails to connect to the socket. Live-fire/Docker-dependent tests and issues must be either structurally validated only (and disclosed as such) or deferred as environment-blocked, never assumed to pass.
- **A Claude Code session cannot delete a branch on this repo** — same restriction class as the wiki: `git push origin --delete <branch>` returns HTTP 403 from the git proxy, and the GitHub MCP server exposes no branch-deletion tool. Branch pruning has to stop at *identifying* which branches are safe to delete (confirmed merged via the PR's `merged_at`, not just `git merge-base --is-ancestor` — squash/rebase merges break literal ancestry) and handing the list to the user to delete themselves. `.github/workflows/delete-merged-branch.yml` (added to close this gap going forward) auto-deletes a PR's head branch on merge via the repo's own `GITHUB_TOKEN`, which isn't subject to this session-level restriction.
- **A Claude Code session cannot change repo-admin settings either** — same restriction class again, confirmed for branch protection (#509): the GitHub MCP server has no branch-protection-update tool, there's no `gh` CLI in this environment, and `scripts/setup/setup_branch_protection.sh` itself requires an admin-scoped token this session doesn't have. Also applies to reading Projects v2 board state (#510): GraphQL (the only API surface that exposes it) isn't reachable either. Both need a human with real admin/browser access; identify and document the gap, don't attempt a workaround.